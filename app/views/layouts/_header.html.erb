<% if Current.user %>
  <div class='flex bg-main-black py-3 z-10 sticky top-0 container mx-auto lg:px-5 xl:w-2/3 lg:w-full sm:w-full px-10'>
    <div class='flex-1 flex items-center'>
      <%= link_to 'Budget', root_path, class: 'font-serif font-bold text-3xl mr-8' %>
    </div>
    <div class='flex items-center'>
      <%= link_to 'Accounts', accounts_path, class: "btn btn-link mr-5  text-lg #{nav_active('accounts')}" %>
      <%= link_to 'Spends', spends_path, class: "btn btn-link mr-5  text-lg #{nav_active('spends')}" %>
      <%= link_to 'Log Out', logout_path, class: "btn btn-link" %>
    </div>
  </div>
<% end %>